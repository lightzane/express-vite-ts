{
  "files": [], // Explicitly empty to avoid including any files by default and prevents warning: must have setting "composite": true on referenced projects.
  "references": [
    { "path": "./tsconfig.server.json" },
    { "path": "./tsconfig.client.json" }
  ]
}
